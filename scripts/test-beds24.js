const { getBeds24Properties, getBeds24Rooms } = require('./.next/server/app/api/beds24/sync/route.js') // wait, maybe that's not accessible

// Better to query the beds24_sync_log or api_logs or just run a script using curl.
