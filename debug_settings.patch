--- tests/e2e/settings.spec.ts
+++ tests/e2e/settings.spec.ts
@@ -57,6 +57,11 @@
     await page.locator('button', { hasText: 'Confirm', exact: true }).click();
     
     // Check for banking update success
+    try {
+      await expect(page.locator('text=Banking details updated securely.')).toBeVisible({ timeout: 5000 });
+    } catch (e) {
+      console.log('Error message in UI:', await page.locator('.bg-red-50').textContent());
+      throw e;
+    }
-    await expect(page.locator('text=Banking details updated securely.')).toBeVisible({ timeout: 10000 });
   });
 });
